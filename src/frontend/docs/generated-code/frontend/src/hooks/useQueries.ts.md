# frontend/src/hooks/useQueries.ts

React Query hooks for managing obstacle events with useStoreObstacleEvent mutation that accepts and persists classification object containing objectType and motion fields, and useGetAllObstacleEvents query for retrieving stored obstacle events.

